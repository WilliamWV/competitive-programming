#include <iostream>
#include <vector>
#include <string>
#include <cmath>

typedef unsigned long long ULL;


int gcd(int a, int b);

int lcm(int a, int b);

std::vector<int> prime_factors(ULL n);

bool is_prime(ULL n);

std::vector<ULL> first_n_primes(int n);

ULL nth_prime(int n);

